# Optional server-side fallbacks.
# Primary source of config is the Settings UI (localStorage),
# posted per-request via an x-aeo-config header.

LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

TINYFISH_API_KEY=
TINYFISH_ENDPOINT=https://api.tinyfish.io

WUNDERGRAPH_ENDPOINT=http://localhost:3000/api

GHOST_ADMIN_URL=
GHOST_ADMIN_KEY=

DATABASE_URL=
